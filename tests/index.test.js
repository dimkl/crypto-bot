describe("e2e", () => {
  test("executes setup mode", async () => {
    expect(true).toBe(true);
  });
  test("executes buy mode", async () => {
    expect(true).toBe(true);
  });
  test("executes sell mode", async () => {
    expect(true).toBe(true);
  });
});